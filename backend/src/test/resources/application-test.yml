spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  cache:
    type: none

app:
  jwt:
    # deterministic for tests; base64 string
    secret: dGVzdC1qd3Qtc2VjcmV0LWtleS0xMTQwOA==
    expiration: 86400000
    refresh-expiration: 604800000
  minio:
    endpoint: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin123
    bucket: study11408
  ai:
    service-url: http://localhost:8000

logging:
  level:
    root: WARN

